import { Button } from "./button";

export const VariantPropsGood = () => {
  return (
    <div>
      <Button size="lg">Primary Button</Button>
      <Button variant="secondary" size="sm">
        Secondary Button
      </Button>
    </div>
  );
};
